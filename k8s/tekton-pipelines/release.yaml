apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tekton-pipelines
spec:
  interval: 5m
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  chart:
    spec:
      chart: tekton-pipelines-pipeline
      version: 1.0.2
      sourceRef:
        kind: HelmRepository
        name: tekton-pipelines
  # https://github.com/cdfoundation/tekton-pipelines-helm-chart/blob/main/charts/tekton-pipelines-pipeline/values.yaml
  values: {}
